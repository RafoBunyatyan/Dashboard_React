import React, {  } from "react";
import "../assets/styles/addForm.css"

function AddForm() {
	return (
		<>
			<form className="form">
				{/* hl@ chem arel functional@;; Sori... -_- )) */}
			</form>
		</>
	);
};

export default AddForm;